{
  "name": "ai-healthcare-root",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "yarn workspace server build",
    "start": "yarn workspace server start"
  }
}
